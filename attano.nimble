# Package

version       = "0.1.0"
author        = "Kier Davis"
description   = "A language for specifying the TTL netlist, and a layout/routing engine to convert netlists to a finished design."
license       = "MIT"
bin           = @["attanoc"]

# Dependencies

requires "nim >= 0.13.0"

